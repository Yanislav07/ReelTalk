@model IEnumerable<ReelTalk.Data.Production>

@{
	Layout = "_Layout";
}

<!DOCTYPE html>

<html>
<head>
	<meta name="viewport" content="width=device-width" />
	<title>Index</title>
</head>
<body>
	<p>
		<a asp-action="Create">Create New</a>
	</p>
@* <div class="container-fluid py-4">
		<div class="row row-cols-2 row-cols-md-3 row-cols-lg-6 g-4">
			@foreach (var item in Model) {
				<div class="col">
					<div class="card h-100 border-0">
						<!-- Card -->
						<img src="@item.ImagePath" class="card-img-top" alt="@item.Title">
						<div class="card-body text-F3E6FC">
							<h5 class="card-title">@item.Title</h5>
							<p class="text-muted">@item.ReleaseYear • @item.Type</p>
						</div>
						<div class="card-footer bg-transparent border-top border-secondary">
                            <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm w-100" style="background-color: #E54829; color: white;">View</a>
						</div>
					</div>
				</div>
                }
			</div>
		</div> *@

	<div class="container-fluid p-4">
		<div class="row row-cols-2 row-cols-md-3 row-cols-lg-6 g-3">
			@foreach (var item in Model)
			{
				<div class="col">
					<!-- Image-as-card with hover effect -->
					<div class="card border-0 bg-transparent">
						<img src="@item.ImagePath"
							 class="card-img rounded-3 shadow"
							 alt="@item.Title"
							 style="
              aspect-ratio: 2/3;
              object-fit: cover;
              transition: transform 0.3s;
				">
					</div>
				</div>
			}
		</div>
	</div>

</body>
</html>
